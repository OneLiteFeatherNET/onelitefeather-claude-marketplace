{
  "name": "context-layer",
  "displayName": "Context Layer",
  "version": "0.1.0",
  "description": "Puts a retrieval layer between docs and code: Serena (LSP symbol search) plus routing skills, so the agent navigates on purpose instead of spamming grep/find/read — cutting API calls and tokens.",
  "author": { "name": "OneLiteFeather", "url": "https://github.com/OneLiteFeatherNET" },
  "license": "MIT",
  "keywords": ["serena", "lsp", "context", "tokens", "retrieval", "jvm"],
  "mcpServers": {
    "serena": {
      "command": "uvx",
      "args": [
        "--from", "git+https://github.com/oraios/serena",
        "serena", "start-mcp-server",
        "--context", "claude-code",
        "--project", "${CLAUDE_PROJECT_DIR}"
      ]
    }
  }
}
